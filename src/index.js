// @ts-check

/**
 *
 * @param {number} a
 * @param {number} b
 */

function add(a, b) {
  // eslint-disable-next-line no-console
  console.log(a + b);
}

add(23, 32); // raises type errors if parameter is other than number.
