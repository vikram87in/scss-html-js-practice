# scss-html-js-practice
This project is for practicing various css, scss, html, js, &amp; module concepts

Install the pacakages with `npm i` and then run command `pnpm run sass` to generate the main.css file